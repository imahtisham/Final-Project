@page
@model Iportfilo.Pages.myadmin.showstdModel
@{
}
<div class="container">
    <div class="row" justify-content-center>
        <div class="col-md-10">
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Degree</th>
                        <th scope="col">startyear</th>
                        <th scope="col">endyear</th>
                        <th scope="col">InsistueName</th>
                        <th scope="col" colspan="2">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.education)
                    {
                        <tr>
                            <th scope="row">@item.Id</th>
                            <td>@item.Degree</td>
                            <td>@item.startyear</td>
                            <td>@item.endyear</td>
                            <td>@item.InsistueName</td>
                            <td>
                                <a class="btn btn-danger" asp-page-handler="Delete" asp-route-Id="@item.Id"> Delete</a>
                            </td>
                            <td>
                                <a class="btn btn-primary" asp-page="eduupdate" asp-route-Id="@item.Id"> Update</a>
                            </td>
                        </tr>
                    }

                </tbody>
            </table>
        </div>
    </div>

</div>
